services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rice_soccer
    ports:
      - "5432:5432"
    volumes:
      - rice_soccer_pgdata:/var/lib/postgresql/data

volumes:
  rice_soccer_pgdata:
